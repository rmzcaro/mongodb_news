// // grab the articles as a json
// $.getJSON("/articles", function(data) {
//     // fr each one
//     for (var i = 0; i < data.length; i++) {
//         // the display the info on the page
//         $(#articles).append("<p data-id'" + data[i]._id + "'>" + data[i]._headline + "'>" + )
//     }
// })